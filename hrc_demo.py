import RPi.GPIO as GPIO
import time
import sys

trig_pin = 15                           # GPIO 15
echo_pin = 14                           # GPIO 14
speed_of_sound = 34370                  # 20?????(cm/s)

GPIO.setmode(GPIO.BCM)                  # GPIO?BCM??????
GPIO.setwarnings(False)                 # BPIO?????
GPIO.setup(trig_pin, GPIO.OUT)          # Trig?????????
GPIO.setup(echo_pin, GPIO.IN)           # Echo?????????

def get_distance(): 
    #Trig???10?s??HIGH???????????
    GPIO.output(trig_pin, GPIO.HIGH)
    time.sleep(0.000010)
    GPIO.output(trig_pin, GPIO.LOW)

    while not GPIO.input(echo_pin):
        pass
    t1 = time.time() # ????????Echo???HIGH?????????

    while GPIO.input(echo_pin):
        pass
    t2 = time.time() # ????????Echo???LOW?????????

    return (t2 - t1) * speed_of_sound / 2 # ???????????????


while True: # ??????
    try:
        distance = '{:.1f}'.format(get_distance())  # ???1?????
        print("Distance: " + distance + "cm")       # ??
        time.sleep(1)                               # 1???

    except KeyboardInterrupt:                       # Ctrl + C??????
        GPIO.cleanup()                              # GPIO????
        sys.exit()                                  # ???????


